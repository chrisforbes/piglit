include_directories(
	${GLEXT_INCLUDE_DIR}
	${OPENGL_INCLUDE_PATH}
)

link_libraries (
	piglitutil_${piglit_target_api}
	${OPENGL_gl_LIBRARY}
	${OPENGL_glu_LIBRARY}
)

piglit_add_executable (arb_texture_gather-minmax minmax.c)
piglit_add_executable (arb_texture_gather-texture-gather-offset-limits texture-gather-offset-limits.c)
piglit_add_executable (arb_texture_gather-textureGather textureGather.c)
